import './List.scss';

const List = ({ data }) => {
    console.log('!!', data);

    return (
        <>
            <div className='list-container'>list test</div>
        </>
    );
};

export default List;
